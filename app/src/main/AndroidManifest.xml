<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.cg.pedglacee">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/atv_login">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/atv_menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MesasActivity"
            android:label="@string/atv_mesas" />
        <activity
            android:name=".NovoPedidoActivity"
            android:label="@string/atv_novo_pedido" />
        <activity
            android:name=".MeusPedidosActivity"
            android:label="@string/atv_meus_pedidos" />
        <activity
            android:name=".DetalhePedidoActivity"
            android:label="@string/atv_comanda" />
        <activity
            android:name=".CadastrosActivity"
            android:label="@string/atv_cadastros"></activity>
    </application>

</manifest>